# TextEditorCompressor

Console-based text editor that takes in commands to insert, delete, move cursor, etc. Compresses .txt to .cmp through a dictionary to save space and decompresses from .cmp to .txt back.
